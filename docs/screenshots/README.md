# Screenshots

Drop the 8 PNGs here with these exact names so the README references resolve:

| File | What it shows |
| --- | --- |
| `1a-agent-config.png` | AI Agent properties: role, plain-English goal, auto-selected tools |
| `1b-agent-run.png` | The agent run: balance read, send + attest, proofs, Telegram, all green |
| `2-guardrail-block.png` | Spend-limit blocks an over-budget agent: red node, "no funds moved" |
| `3a-build-from-prompt.png` | A sentence typed in the command bar (empty canvas) |
| `3b-generated-run.png` | The generated Wallet → Send CSPR → Telegram flow, run for real |
| `4a-conditional-prompt.png` | The conditional multi-wallet instruction in the command bar |
| `4b-conditional-run.png` | The agent pays only wallets under the threshold and explains why |
| `5-journal.png` | The Journal: aligned ledger + GitHub-style activity calendar |

All captured on Casper testnet. PNG preferred; keep them reasonably sized (under ~1 MB each is plenty for a README).
